# FoxBear Delivery Rules

완료 결과는 `결과 보고` 제목 아래 다음 세 구역을 번호와 순서 그대로 사용한다. 사용자가 별도 형식을 명시하지 않는 한 다른 독립 구역을 추가하지 않는다.

## 1. 적용 내역

- 분석 범위, 수정 파일, 해결한 오류·버그·예외를 기록한다.
- 성능·기술·기능·엔진 영향과 실행한 검증 결과를 기록한다.
- 실행 환경 때문에 완료하지 못한 검증은 숨기지 않고 명시한다.

## 2. 다음 패치 예정

- 다음 우선순위와 후속 개선 후보를 기록한다.
- 실제 브라우저, 기기, Firebase 배포 등 외부 환경에서 확인해야 할 게이트를 기록한다.

## 3. 다운로드 파일 2종

- GitHub Desktop에 적용할 전체 프로젝트 `-full.zip`을 제공한다.
- 저장소 루트에 압축을 풀고 같은 파일을 덮어쓰는 변경 파일 전용 `-patch.zip`을 함께 제공한다.
- 두 ZIP 모두 `.git`, `node_modules`, `.firebase`, 실제 `.firebaserc`, 비밀 환경 파일, 캐시, 생성 테스트 결과를 포함하지 않는다.
- 패치 ZIP에는 `PATCH_NOTES.md`와 `DELETE_PATHS.txt`를 포함한다.
