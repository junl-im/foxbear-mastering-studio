# v1.6.97 Boot Payload & Delivery Privacy Hardening

## Scope
- Remove redundant main-thread JSZip boot loading; ZIP encoding remains Worker-owned.
- Keep JSZip service-worker precaching so offline ZIP Worker startup still works.
- Stop persisting raw SMTP response text and operations recipient in user-readable incident report delivery state.
- Keep user-visible delivery status, message ID, subject, sender name, counts, and accepted timestamp.
- Add an initial script-count regression budget and privacy regression guard.
