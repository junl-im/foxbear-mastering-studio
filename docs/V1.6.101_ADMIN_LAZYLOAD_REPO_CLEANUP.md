# v1.6.101 Admin lazy-load and repository cleanup

The administrator incident operations JavaScript is loaded only when the incident tab is opened; shared admin CSS remains eager so visit-statistics tabs never flash unstyled. Dynamic script loading keeps SHA-384 integrity and the existing exact Trusted Types allowlist. Temporary Spectrum deletion helpers are removed through the normal cleanup contract.
