# v1.5.95 Product Introduction, Popup Consistency, and Mail-Test Recovery

- Replaces the hidden support-heavy version popup with a user-facing explanation of smart analysis, quality protection, A/B preview, batch processing, export support, and local audio privacy.
- Keeps incident reporting and memory/performance diagnostics as separate actions in the header settings panel.
- Uses a 32px shared circular close control with a 10px centered glyph and reserved title spacing.
- Adds a generic `role=dialog` backdrop fallback that activates each popup's own close control when the user clicks outside the panel.
- Moves the new popup styling into `assets/css/components/support-settings.css` so `studio.css` remains under its modular size budget.
- Changes incident persistence to create first and read only after a denied duplicate write, preventing the first real mail test from reading an owner-only document that does not exist.
- Keeps real mail testing single-flight with `aria-busy` and actionable permission/deployment guidance.
- Regression: `node qa/v1595_popup_settings_mail_test_recovery_smoke.js`.
