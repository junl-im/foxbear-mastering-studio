# v1.6.106 Browser Geometry / Flaky History Recovery

## Incident

GitHub Actions run `32103778747` failed the Browser release gate on `chromium-desktop` Runtime Health. The same value, `centerSpread = 8.0078125`, repeated in the initial attempt, Playwright retry, and failed-only retry against an exact `<= 8` assertion. Mobile Runtime Health passed.

The additional retry/history annotations were downstream consequences of that single repeated browser case. A separate history defect could also retain an older unresolved observation because clean primary passes were not fed back into flaky-history state.

## Fix

- Retain the 8px Header center-spread design target.
- Compare only the excess above 8px and allow at most one CSS layout quantum (`1/64px`).
- Preserve named center coordinates for build, device, studio, designer, mode switch, and settings toggle in diagnostics.
- Add `primaryPassed` observations to the retry summary.
- Use primary-pass observations only to resolve matching existing history entries; do not create history records for ordinary healthy tests.

## Regression contract

- `8.0078125px`: PASS because excess is `0.0078125px`, below `1/64px`.
- `8.02px`: FAIL because excess is larger than `1/64px`.
- Previously repeated tracked case + matching next primary PASS: unresolved count becomes zero.
- Fresh primary PASS with no history entry: no new flaky-history record.

## Local verification

- Static/behavioral QA: 462/462 PASS.
- Static release gate: PASS.
- Browser fixture preflight: PASS.
- Firebase Hosting payload boundary: 162 files PASS.
- Functions syntax: PASS.
- Full Playwright v1.6.106 execution remains for GitHub Actions after push because the review container does not include the browser runtime.
