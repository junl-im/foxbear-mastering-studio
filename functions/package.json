{
  "name": "foxbear-incident-mailer",
  "version": "1.6.89",
  "private": true,
  "description": "Optional Blaze-plan incident delivery functions. Spark administrator access is handled by Firebase Google Auth and Firestore Rules without Cloud Functions.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js && node --check app-check-policy.js",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0",
    "nodemailer": "9.0.3"
  }
}
