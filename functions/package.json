{
  "name": "foxbear-incident-mailer",
  "version": "1.5.69",
  "private": true,
  "description": "Server-side incident delivery with recorded mail tests, inbox/spam receipt confirmation, verification freshness telemetry, unified branded email templates, recovery, audit, webhook failover, and SMTP diagnostics.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0",
    "nodemailer": "9.0.3"
  }
}
