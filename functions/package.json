{
  "name": "foxbear-incident-mailer",
  "version": "1.5.64",
  "private": true,
  "description": "Server-side incident delivery with scheduled SMTP/Secret health checks, stale queue and dead-letter alerts, KST quotas, fenced leases, and summary recovery for FoxBear Mastering Studio.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0",
    "nodemailer": "9.0.3"
  }
}
