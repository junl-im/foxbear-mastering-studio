{
  "name": "foxbear-incident-mailer",
  "version": "1.6.1",
  "private": true,
  "description": "Server-side incident delivery with callable-first authenticated report submission, delivery status lookup, mail recovery, and operations diagnostics.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0",
    "nodemailer": "9.0.3"
  }
}
